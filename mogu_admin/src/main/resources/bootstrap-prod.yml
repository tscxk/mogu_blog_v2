spring:
  cloud:
    nacos:
      discovery:
        server-addr: ${server.ip}:8848
        namespace: prod
      config:
        server-addr: ${server.ip}:8848
        #文件后缀
        file-extension: yaml
        #指定分组
        group: prod
        #指定命名空间
        namespace: prod


